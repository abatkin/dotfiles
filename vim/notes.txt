To setup YouCompleteMe, use something like:
./install.py --clang-completer --go-completer --rust-completer --java-completer --ts-completer

You will also need to install: cmake gcc-g++ python3-devel npm
(in addition to some basic development tools from "groupinstall 'development tools'")

